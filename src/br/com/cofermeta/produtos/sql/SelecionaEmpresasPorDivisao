SELECT EMP.CODEMP,
       TGF.LOCALPAD
FROM sankhya.TSIEMP EMP
INNER JOIN sankhya.TGFEMP TGF ON EMP.CODEMP = TGF.CODEMP
WHERE EXISTS
      (SELECT 1
      FROM sankhya.TSIEMP EMP1
      WHERE  EMP.CODEMP = EMP1.CODEMP
      AND AD_CODDIVISAO IN (:CODDIVISAO))